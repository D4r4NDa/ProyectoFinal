<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ProyectoFinal_David_Rodriguez_Aranda" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/red</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="RoundCornerDialog" parent="@style/Theme.MaterialComponents.Dialog">
        <item name="android:textColorPrimary">#000000</item>
        <item name="android:textColor">#000000</item>
        <item name="android:textColorSecondary">#000000</item>
        <item name="fontFamily">@font/caveat</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="CustomActionBar" parent="@style/Widget.AppCompat.ActionBar">
        <!-- Customize the background color -->
        <item name="android:background">@color/red</item>

        <!-- Customize the title text color -->
        <item name="android:titleTextStyle">@style/CustomActionBarTitle</item>
    </style>

    <style name="CustomActionBarTitle" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <!-- Customize the text color -->
        <item name="android:textColor">@color/white</item>
        <item name="fontFamily">@font/caveat</item>
        <item name="android:textAlignment">center</item>
    </style>

</resources>